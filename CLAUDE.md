@~/.claude/td-fly.md

# td-fly

The lean rebuild of td-flow — `close` + `mailbox` + the `@import` contract, shipped as a
Claude Code plugin. The `@import` line above is live (this repo dogfoods the contract).
The `## Cross-repo` section below is an empty template — td-fly files into no other repo
yet; add a slug there if that changes. See README.md for the build status.

## Cross-repo
<!-- one repo slug per line below; this list scopes /td-fly:mailbox outbound -->
