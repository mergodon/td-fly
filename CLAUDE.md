# td-fly

A lean work rhythm — `close` + `mailbox` + the cross-repo GitHub-issues protocol, shipped
as a **self-contained** Claude Code plugin. No `@import`, no symlink — the commands carry
their own protocol; `contract.md` is the human-readable reference.
The `## Cross-repo` section below is an empty template — td-fly files into no other repo
yet; add a slug there if that changes. See README.md for the build status.

## Cross-repo
<!-- one owner/name slug per line below; this list scopes /td-fly:mailbox outbound -->
