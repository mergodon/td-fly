@~/.claude/td-fly.md

# td-fly

The lean rebuild of td-flow — `close` + `mailbox` + the `@import` contract, shipped as a
Claude Code plugin. This repo is also the reference example of the contract in use: the
`@import` line above pulls in the shared rhythm, and the section below declares this
project's cross-repo connections. See README.md for the build status.

## Cross-repo
<!-- one repo slug per line below; this list scopes /td-fly:mailbox outbound -->
